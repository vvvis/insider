version: 2.1
jobs:
  build:
    docker:
      - image: circleci/golang
    steps:
      - checkout
      - run: 
          name: Create build
          command: |
            sudo mkdir /bin/insider
            make
            sudo cp insider /bin/insider 
      - store_artifacts:
          path: /bin/insider
          destination: insider
